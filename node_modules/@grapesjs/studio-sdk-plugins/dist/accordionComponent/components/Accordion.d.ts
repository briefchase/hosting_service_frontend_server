import { Editor } from '../../types';
import { AccordionComponentOptions } from '../types';
declare const loadAccordion: (editor: Editor, opts: AccordionComponentOptions) => () => void;
export default loadAccordion;
