import { Editor } from '../../types';
import { AccordionComponentOptions } from '../types';
declare const loadAccordionGroup: (editor: Editor, opts: AccordionComponentOptions) => () => void;
export default loadAccordionGroup;
