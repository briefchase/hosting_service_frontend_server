import { Editor } from '../../types';
import { AccordionComponentOptions } from '../types';
declare const loadAccordionHeader: (editor: Editor, opts: AccordionComponentOptions) => () => void;
export default loadAccordionHeader;
